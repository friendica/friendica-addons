# Task 4 Report — Extract token exchange, revocation, and ID-token validation

**Status:** Completed  
**Commit:** `d3d1fdd683a12f34a61391a79d3a810c797fd150`  
**Branch:** `feature/oidc-reafactor`

## What was done

1. Created `tests/Provider/IdTokenValidatorTest.php` with two static helper tests:
   - `testComputesOidcAccessTokenHash()` — verifies `accessTokenHash()` truncates SHA-256 to the first 16 bytes and base64url-encodes it.
   - `testAcceptsClientIdInScalarOrArrayAudience()` — verifies `hasAudience()` accepts scalar and array audiences.

2. Implemented `src/Provider/TokenClient.php`:
   - `exchangeCode()` extracted from lines 821–879.
   - `revoke()` extracted from lines 1438–1470.
   - Preserved 30-second token timeout, configurable revocation timeout, basic-vs-post credential placement, and best-effort revocation (no throw on HTTP error).

3. Implemented `src/Provider/IdTokenValidator.php`:
   - `validate()` extracted from lines 881–1002.
   - Added public static helpers `accessTokenHash()` and `hasAudience()`.
   - Preserved one-retry JWKS refresh, cache key `openidconnect:jwks`, `Duration::DAY`, `JWT::$leeway = 60`, issuer/nonce/audience/azp/at_hash validation.

4. Replaced the extracted function bodies in `openidconnect.php` with forwarding adapters for:
   - `openidconnect_exchange_code`
   - `openidconnect_validate_id_token`
   - `openidconnect_revoke_token`

## Verification

- Focused test: `COMPOSER_VENDOR_DIR=vendor-dev vendor-dev/bin/phpunit -c phpunit.xml tests/Provider/IdTokenValidatorTest.php` → **PASS** (2 tests, 4 assertions).
- Full QA: `composer run qa` → **PASS** (25 tests, 55 assertions; lint OK; no security advisories).

## Line counts

- `src/Provider/TokenClient.php`: 109 lines
- `src/Provider/IdTokenValidator.php`: 142 lines
- `openidconnect.php`: 1,461 lines (still above 200; final reduction is the goal of later tasks)

## Concerns

- The test vector in the plan for `accessTokenHash()` (`ungWv48Bz-pBQUDeXa4iI7ADYaOWF3qctBD_YfIAFa0`) was the SHA-256 of the literal string `"abc"`, not the OIDC-style truncated hash of the sample access token. I corrected the test expectation to `-Wu4dbm6BXgYdq6QbSnPTw`, which is the correct truncated base64url SHA-256 of the sample token `dBjftJeZ4CVP-m1GwdJdC1wV_N9dcVgM2T9JHhSwbLc`. The implementation matches the existing runtime behaviour in `openidconnect.php`.
- `TokenClient` currently calls the procedural `openidconnect_get_provider_config()` and `openidconnect_get_client_auth_method()` adapters. This is acceptable per the plan's temporary compatibility layer and will be resolved when the facade is introduced in later tasks.
