{
    "name": "friendica/openidconnect",
    "version": "0.2.0",
    "require": {
        "firebase/php-jwt": "^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "scripts": {
        "test": "phpunit -c phpunit.xml"
    },
    "config": {
        "vendor-dir": "vendor"
    }
}
