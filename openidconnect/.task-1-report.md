# Phase 0 — Task 1 Report

## Status
Completed successfully.

## Commit
- **Hash:** `0d854105163df0af866f15d750e774faca7d224a`
- **Message:** `refactor(openidconnect): add PSR-4 addon boundary`

## Baseline
- `composer run qa`: PASS (16 tests, 40 assertions)
- `wc -l openidconnect.php`: 1783 lines

## Changes Made
1. Added PSR-4 autoload mapping in `composer.json`:
   - `Friendica\Addon\OpenIdConnect\` → `src/`
   - `Friendica\Addon\OpenIdConnect\Tests\` → `tests/` (dev)
2. Created `tests/Support/AddonTestCase.php` with shared `setUp()` resetting DI test state and request globals.
3. Created `tests/Support/AutoloadTest.php` verifying `LoginPolicy` is autoloadable.
4. Created minimal `src/Auth/LoginPolicy.php`.
5. Migrated `tests/OpenIdConnectTest.php` to extend `AddonTestCase` and removed its local `setUp()`; preserved `use Friendica\DI`.
6. `tests/bootstrap.php` already required Composer autoload first, then stubs, then `openidconnect.php`; no change needed.

## Verification
- Focused autoload test: PASS
- `composer dump-autoload`: regenerated
- `composer run qa`: PASS (17 tests, 41 assertions)

## Concerns
- The plan's Step 4 wording for `tests/bootstrap.php` was already satisfied by the existing file; no edit was required.
- Added `autoload-dev` for the test namespace because the plan only specified production PSR-4 mapping; without it, `AddonTestCase` was not resolvable during PHPUnit suite loading.
