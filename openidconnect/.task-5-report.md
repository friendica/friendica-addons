# Task 5 Report — Extract userinfo and account-link persistence

## Status
Completed.

## Commit
`056575bd459e945aadad1345633505eff36302f4`

`refactor(openidconnect): extract identity and account linking`

## What was done
1. Created `tests/Identity/UserInfoTest.php` and `tests/Account/AccountLinkerTest.php` with the tests specified in the plan.
2. Implemented `src/Identity/UserInfo.php`:
   - `fromIdToken(object $claims): array` — scalar coercion, `preferred_username` fallback to `nickname`, optional `email_verified` conversion.
   - `fetch(string $accessToken): array` — bearer header, 30-second timeout, provider endpoint lookup via `ProviderConfiguration`, empty-array failure contract.
3. Implemented `src/Account/AccountLinker.php`:
   - `get(int $uid): ?array`
   - `link(int $uid, string $sub, string $email, string $nickname): bool` — rejects empty subject and subjects owned by another user.
   - `unlink(int $uid): void` — clears `user.openid`, deletes the three `openidconnect` pconfig values, removes `openidconnect_tokens`.
4. Replaced the old helper bodies in `openidconnect.php` with forwarding adapters to the new services.
5. Updated the `unlink` route to delegate persistence/session removal to `AccountLinker::unlink()`.

## Verification
- `composer run qa` passes: lint OK, PHPUnit 27 tests / 58 assertions OK, composer audit clean.
- New files are under 200 LOC:
  - `src/Identity/UserInfo.php`: 96 LOC
  - `src/Account/AccountLinker.php`: 58 LOC
  - `openidconnect.php`: 1367 LOC (still above target; final cleanup is Task 12).

## Concerns
- The `UserInfo::fetch()` method calls `DI::httpClient()->get()`. The current test stub only exposes `fetch()` and `post()`; the existing `openidconnect.php` and `TokenClient` already use `get()`/`post()` as well, so this matches the established test boundary. No focused test exercises `UserInfo::fetch()` yet.
- `AccountLinker::unlink()` does not check whether a link exists before clearing state; this mirrors the original persistence block exactly.
