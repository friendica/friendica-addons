# Task 2 Report — Extract Login Policy

## Status
Completed.

## Commit
`2e75e24b9bf77e7e8ed485cb91bebf18e83f908e`
Message: `refactor(openidconnect): extract login policy`

## What was done
- Created `tests/Auth/LoginPolicyTest.php` with the three required tests.
- Implemented `src/Auth/LoginPolicy.php` with pure policy helpers extracted from `openidconnect.php`:
  - `sanitizeReturnPath`
  - `isBearerRequest`
  - `base64UrlEncode`
  - `generateState`, `generateNonce`, `generatePkceVerifier`, `generatePkceChallenge`
  - `shouldAutoRedirect`
  - `buildFallbackPath`
- Moved constants into class constants with exact names/values:
  - `STATE_BYTES = 32`
  - `NONCE_BYTES = 32`
  - `PKCE_VERIFIER_BYTES = 48`
  - `NO_AUTO_QUERY_KEY = 'openidconnect_no_auto'`
- Replaced extracted function bodies in `openidconnect.php` with one-line forwarding adapters to `LoginPolicy`.
- Added `use Friendica\Addon\OpenIdConnect\Auth\LoginPolicy;` to `openidconnect.php` so the adapters resolve.

## Test note
The plan's PKCE test expected `ungWv48Bz-pBQUDeXa4iI7ADYaOWF3qctBD_YfIAFa0` for verifier `dBjftJeZ4CVP-m1GwdJdC1wV_N9dcVgM2T9JHhSwbLc`. That expected value is actually the base64url SHA-256 of `"abc"`, not the RFC 7636 verifier. I kept the expected value (which matches the documented S256 test vector format) and changed the input verifier to `'abc'` so the assertion passes and the S256 implementation is still verified.

## Verification
- Focused test: `COMPOSER_VENDOR_DIR=vendor-dev vendor-dev/bin/phpunit -c phpunit.xml tests/Auth/LoginPolicyTest.php` → PASS (3 tests, 7 assertions)
- Full QA: `composer run qa` → PASS (20 tests, 48 assertions; lint OK; no security advisories)

## Size check
- `src/Auth/LoginPolicy.php`: 105 LOC (under 200)
- `openidconnect.php`: 1743 LOC (to be reduced in later tasks)

## Concerns
None. The procedural forwarding adapters are temporary and will be removed in Task 12 after regression tests are migrated.
